<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.b2.Sinnanda.mapper.QnaMapper">
	
	<!-- [조회] Board Map -->
	<resultMap type="com.b2.Sinnanda.vo.Qna" id="resultQnaMap">
		<id column="qna_no" property="qnaNo"/>
		<result column="member_no" property="memberNo"/>
		<result column="qna_title" property="qnaTitle"/>
		<result column="qna_title" property="qnaSecret"/>
		<result column="create_date" property="createDate"/>
	</resultMap>

	<!-- [이승준] QnA 목록 조회 -->
	<select id="selectQnaList" parameterType="Map" resultMap="resultQnaMap">
		SELECT 
			qna_no, 
			member_no, 
			qna_title, 
			qna_secret, 
			create_date
		FROM qna 
		ORDER BY create_date DESC
		LIMIT #{beginRow}, #{rowPerPage}
	</select>
	<!-- [이승준] QnA 갯수 조회-->
	<select id="selectQnaTotalCount" resultType="int">
		SELECT 
			COUNT(*) 
		FROM qna
	</select>
</mapper>